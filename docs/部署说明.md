# 部署说明

## 1. 环境要求

| 依赖    | 版本         |
| ------- | ------------ |
| Node.js | >= 22        |
| Java    | >= 21        |
| Maven   | >= 3.9       |
| Docker  | >= 29        |
| MySQL   | 8.0 (Docker) |

## 2. 配置说明

| 配置项     | 值                                  | 说明              |
| ---------- | ----------------------------------- | ----------------- |
| 前端端口   | 3001                                | Vite 开发服务器   |
| 后端端口   | 8081                                | Spring Boot       |
| MySQL端口  | 3307                                | Docker映射        |
| 数据库名   | defect_triage                       | —                 |
| MySQL用户  | root                                | —                 |
| MySQL密码  | root123                             | —                 |
| AI API Key | sk-b28baac32f224155bbe69844264c4ec3 | deepseek-v4-flash |

## 3. 本地启动步骤

```bash
# 1. 启动 MySQL
docker run -d --name defect-triage-mysql \
  -e MYSQL_ROOT_PASSWORD=root123 \
  -e MYSQL_DATABASE=defect_triage \
  -p 3307:3306 mysql:8.0

# 2. 启动后端（端口 8081）
cd backend
mvn spring-boot:run

# 3. 启动前端（端口 3001）
cd source/frontend
npm install
npm run dev

# 4. 访问
open http://localhost:3001
```

## 4. Docker Compose 启动（可选）

```bash
docker compose up -d
# MySQL: 3307, 后端+前端: 8081
```

## 5. 演示数据

系统启动时自动初始化：

**预置账号（密码均为 admin123）：**

| 用户名    | 角色   |
| --------- | ------ |
| submitter | 提交人 |
| engineer  | 工程师 |
| qa        | QA     |

**预置缺陷：** 177 条，覆盖全部 10 种状态（DRAFT / REPORTED / TRIAGING / ANALYZED / PLANNED / IN_REPAIR / FIXED / VERIFIED / CLOSED / REOPENED），包含安全、性能、兼容性、运维等真实场景

**预置知识条目：** 每条 CLOSED 缺陷自动生成 3 条（回归用例 / 排查手册 / 风险规则）

## 6. 测试执行方式

```bash
# 后端测试
cd backend
mvn test

# 前端测试
cd source/frontend
npm test

# E2E测试
cd source/frontend
node e2e-test.mjs
```

## 7. 常见问题

| 问题             | 解决方式                                                             |
| ---------------- | -------------------------------------------------------------------- |
| 端口 3307 被占用 | 修改 docker run -p 端口和 application.properties 中的 datasource url |
| MySQL 连接失败   | 确认 Docker 容器已启动: `docker ps`                                  |
| AI 建议不生成    | 检查 DeepSeek API Key 是否正确                                       |
| 前端页面空白     | 清除浏览器 localStorage 后刷新                                       |
